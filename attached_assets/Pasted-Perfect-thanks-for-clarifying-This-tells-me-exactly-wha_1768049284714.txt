Perfect — thanks for clarifying 👍
This tells me **exactly** what’s going on.

Right now, your CMS is **only displaying content**, but it is **NOT storing it anywhere persistently**. That’s why:

* You don’t see `database.json`
* You don’t see `posts = []`
* Search returns **no articles**
* Content appears only after posting, but isn’t queryable

👉 In short: **your CMS is missing a real “content storage layer.”**

Let me explain this **very clearly**, then give you a **copy-paste working solution for Replit**.

---

# 🧠 WHAT YOUR CURRENT CMS REALLY IS

Right now, your CMS is behaving like this:

```
Admin writes content
↓
Server renders content directly
↓
No permanent storage
↓
Search has nothing to search
```

So when you click **Search**, the backend says:

> “I received ‘IPO’, but I have no stored articles to check against.”

---

# ✅ THE SIMPLEST FIX (NO DATABASE, NO COMPLEXITY)

We’ll add **ONE file**:

```
posts.json
```

This will act as your **content store** (like Trade Brains does internally).

✔ Easy
✔ Replit-friendly
✔ Search works
✔ Edit / delete works
✔ Category-wise posts work

---

# 🧩 STEP 1: CREATE `posts.json`

In Replit → **New File** → name it:

```
posts.json
```

Paste this:

```json
[]
```

Save it.

---

# 🧩 STEP 2: ADD THIS TO YOUR `server.js`

### 🔹 At the TOP of the file

```js
const fs = require("fs");
```

---

### 🔹 Helper functions (PASTE ONCE)

```js
function readPosts() {
  return JSON.parse(fs.readFileSync("posts.json", "utf8"));
}

function savePosts(posts) {
  fs.writeFileSync("posts.json", JSON.stringify(posts, null, 2));
}
```

---

# 🧩 STEP 3: FIX YOUR CMS POST ROUTE

### 🔹 Replace your “add post” logic with THIS:

```js
app.post("/admin/add-post", (req, res) => {
  const posts = readPosts();

  const newPost = {
    id: Date.now(),
    title: req.body.title,
    content: req.body.content,
    category: req.body.category,
    status: "published",
    createdAt: new Date().toISOString()
  };

  posts.push(newPost);
  savePosts(posts);

  res.redirect("/admin");
});
```

✔ Now your content is **actually saved**

---

# 🧩 STEP 4: FIX SEARCH (THIS WILL NOW WORK)

```js
app.get("/search", (req, res) => {
  const query = req.query.q?.toLowerCase().trim();
  if (!query) return res.send("No search term");

  const posts = readPosts();

  const results = posts.filter(post => {
    const text =
      post.title +
      " " +
      post.content.replace(/<[^>]*>/g, "");

    return (
      post.status === "published" &&
      text.toLowerCase().includes(query)
    );
  });

  let html = `<h2>Search results for "${req.query.q}"</h2>`;

  if (results.length === 0) {
    html += `<p>No articles found.</p>`;
  }

  results.forEach(post => {
    html += `
      <div>
        <h3>${post.title}</h3>
        <small>${new Date(post.createdAt).toLocaleString()}</small>
        <p>${post.content.replace(/<[^>]*>/g,"").substring(0,150)}...</p>
        <a href="/post/${post.id}">Read more</a>
      </div>
      <hr>
    `;
  });

  res.send(html);
});
```

---

# 🧩 STEP 5: DISPLAY FULL POST PAGE

```js
app.get("/post/:id", (req, res) => {
  const posts = readPosts();
  const post = posts.find(p => p.id == req.params.id);

  if (!post) return res.send("Post not found");

  res.send(`
    <h1>${post.title}</h1>
    <small>${new Date(post.createdAt).toLocaleString()}</small>
    <div>${post.content}</div>
  `);
});
```

---

# 🧠 NOW YOUR WEBSITE WORKS LIKE THIS

```
Admin writes content in CMS
↓
Saved to posts.json
↓
Visible on website
↓
Search works
↓
Related articles can be shown
↓
Edit / delete possible
```

---

# 🔥 THIS IS EXACTLY HOW SITES LIKE TRADE BRAINS STARTED

Trade Brains today uses:

* Database
* Editors
* Search indexing

But conceptually:

> **CMS → Stored posts → Search → Read More**

You now have the same **foundation**.

---

## 🚀 NEXT FEATURES (I CAN ADD FOR YOU)

* Edit / delete in admin panel
* Category-wise posting (IPO, Large Cap, etc.)
* “Read next” / Related articles
* SEO URLs
* Pagination
* Draft vs Published
* Image uploads

Just tell me **what you want next**, and I’ll give you **copy-paste code** for Replit 💪