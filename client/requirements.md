## Packages
framer-motion | Page transitions and UI animations
recharts | Stock charts and market data visualization
date-fns | Date formatting for articles
react-hook-form | Form handling for CMS
@hookform/resolvers | Zod validation for forms
zod | Schema validation
clsx | Class name utility
tailwind-merge | Class name utility

## Notes
Authentication uses Replit Auth (useAuth hook).
API endpoints are defined in @shared/routes.
Images use Unsplash placeholders.
